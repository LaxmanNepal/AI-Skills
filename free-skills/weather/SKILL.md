---
name: free-weather
description: Current weather and forecast using Open-Meteo, no API key.
---
# Free Weather
Call `run_js` with `index.html` and `{ "city": "Kathmandu" }`. You may also pass `lat` and `lon`.