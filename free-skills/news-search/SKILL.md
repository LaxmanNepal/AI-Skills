---
name: free-news-search
description: Search recent headlines through public Google News RSS data without an API key.
---
# Free News Search
Call `run_js` with `index.html` and `{ "query": "Nepal" }`. Return titles, dates and links.