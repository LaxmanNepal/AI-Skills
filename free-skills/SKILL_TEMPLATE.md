---
name: free-example-skill
description: Short, user-facing description of the skill.
---

# Skill Name

## Purpose
Explain what this skill does and when to use it.

## Invocation
Call `run_js` with `scripts/index.html` and a JSON object containing the documented input.

## Input
Document required and optional JSON fields and their types.

## Output
Return a JSON string containing either `{ "result": ... }` or `{ "error": "..." }`.

## Errors
Document validation failures, unavailable network resources, and other expected errors.

## Network & Privacy
State whether the skill needs internet access, what data leaves the device, and whether secrets or API keys are required. Never embed secrets in the skill.
