---
name: free-url-tools
description: Create and normalize safe URL links locally without an API key.
---
# Free URL Tools
Call `run_js` with `{ "url":"https://example.com" }`. Return the normalized URL and encoded form.